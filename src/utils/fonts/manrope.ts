import { Manrope } from 'next/font/google';

const manrope = Manrope({
  subsets: ['latin'],
  weight: ['400', '700'],
  style: 'normal',
  preload: true,
  display: 'swap',
});

export default manrope;
